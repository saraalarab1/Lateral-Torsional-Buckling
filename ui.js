function update_beam_length(length) {
    // beam.scale.set(params.length, params.height, params.depth);
    // redraw_supports();
    console.log(length)
}

function update_beam_height(height) {
    // beam.scale.set(params.length, params.height, params.depth);
    // redraw_supports();
    console.log(height)
}

function update_beam_depth(depth) {
    // beam.scale.set(params.length, params.height, params.depth);
    // redraw_supports();
    console.log(depth)
}